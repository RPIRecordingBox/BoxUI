import whisper
import time

"""
print("Loading speech rec model...")
model = whisper.load_model("base")

def rec(file):
    try:
        start = time.process_time()
        result = model.transcribe(file, fp16=False, language='English')
        print("  > " + result["text"] + f"  ({time.process_time() - start}s)")
    except Exception as e:
        print(f"  > Unknown speech recognition, error: {e}")
"""
